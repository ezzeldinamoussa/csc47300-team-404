{
  "name": "frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "dev": "tsc -p ./tsconfig.json --watch",
    "start": "npx live-server ../dist/frontend"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
